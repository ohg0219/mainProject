<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="NoticeDAO">
	<insert id="insertNotice" parameterType="com.thisisthat.admin.notice.vo.NoticeVO">
		insert into notice_board(board_title,board_content,board_group) 
		values(#{board_title},#{board_content},#{board_group})
	</insert>
	<update id="updateNotice">
		update notice_board set board_title=#{board_title},board_content=#{board_content} where board_no={board_no}
	</update>
	<update id="updatecnt">
		update notice_board set board_cnt=board_cnt+1 where board_no=#{board_no}";
	</update>
	<delete id="deleteNotice">
		delete from notice_board board_no=#{board_no}
	</delete>
	<select id="getNotice">
		select * from notice_board where board_no=#{board_no}
	</select>
	<select id="noticeList" resultType="com.thisisthat.admin.notice.vo.NoticeVO">
		select * from notice_board where board_group=#{board_group} order by board_no desc
	</select>
	
	
</mapper>
